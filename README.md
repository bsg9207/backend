# directory 구조

```
root
└─ src/            // project 관련
    ├─ api/        // api
    ├─ build/      // build [config(dev,qa,live,keystore), docker(dev,qa,live), main.go]
    ├─ common/     // common
    ├─ internal/   // 내부 module(in build) 에서만 사용하는 code들
    └─ rest/       // api test
```
